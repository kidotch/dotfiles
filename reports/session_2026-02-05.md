
いい質問。**それ、今やっておくと将来の自分を何十回も救うやつ**。

結論から言うと👇
👉 **「1コマンドで再現できるスクリプト」＋「dotfiles 管理」**がベスト
👉 Vast / VPS / ローカル / どこでも同じ体験にできる

以下、**実運用で一番ラクな構成**を出すね。

---

## ① 最強にラク：`setup-locale-tmux.sh` を作る（即効）

まずは「今回ハマった日本語問題」を**完全自動化**。

### スクリプト作成

```bash
cat << 'EOF' > setup-locale-tmux.sh
#!/usr/bin/env bash
set -e

echo "[1/5] Install locales"
apt update
apt install -y locales

echo "[2/5] Generate C.UTF-8"
locale-gen C.UTF-8

echo "[3/5] Set default locale"
update-locale LANG=C.UTF-8

echo "[4/5] Export locale for current shell"
export LANG=C.UTF-8
export LC_ALL=C.UTF-8

echo "[5/5] Kill tmux server (if exists)"
tmux kill-server 2>/dev/null || true

echo "Done. Please re-SSH and start tmux again."
EOF
```

```bash
chmod +x setup-locale-tmux.sh
```

### 以後どの環境でも

```bash
sudo ./setup-locale-tmux.sh
```

👉 **これ一発で今回の地獄は再発しない**。

---

## ② `.tmux.conf` もテンプレ化（vi前提・安定版）

```bash
cat << 'EOF' > ~/.tmux.conf
# vi keybindings
set -g mode-keys vi

# copy-mode-vi bindings
bind -T copy-mode-vi v     send -X begin-selection
bind -T copy-mode-vi Space send -X begin-selection
bind -T copy-mode-vi y     send -X copy-selection

# recommended
set -g mouse on
set -g history-limit 100000
EOF
```

反映：

```bash
tmux source-file ~/.tmux.conf
```

---

## ③ dotfiles リポジトリを作る（中長期の正解）

### 構成例（超おすすめ）

```text
dotfiles/
├── tmux/
│   └── tmux.conf
├── scripts/
│   └── setup-locale-tmux.sh
├── bash/
│   └── bashrc
├── install.sh
```

### `install.sh`

```bash
#!/usr/bin/env bash
set -e

ln -sf $(pwd)/tmux/tmux.conf ~/.tmux.conf
ln -sf $(pwd)/scripts/setup-locale-tmux.sh ~/setup-locale-tmux.sh

echo "Dotfiles installed."
```

### 新環境では

```bash
git clone https://github.com/yourname/dotfiles.git
cd dotfiles
./install.sh
sudo ./scripts/setup-locale-tmux.sh
```

👉 **Vast / RunPod / VPS / ローカル全部同じ**

---

## ④ Vast.ai 専用なら「On-start Script」もアリ

Vast の **On-start Script** にこれを入れると：

```bash
export LANG=C.UTF-8
export LC_ALL=C.UTF-8
```

※ ただし **tmux kill-server は入れない**（起動時に tmux まだ無いから）

---

## ⑤ Claude Code / TUI 系との相性ベストプラクティス（覚書）

* locale：**C.UTF-8**
* tmux：vi + copy-mode-vi 明示
* TERM：`xterm-256color`
* フォント：日本語対応（ローカル）

👉 この組み合わせは **事故らない黄金構成**。

---

## まとめ（おすすめ優先度）

1. ⭐ **setup スクリプト化**（最優先）
2. ⭐ **.tmux.conf テンプレ**
3. ⭐ **dotfiles Git 管理**
4. Vast なら On-start Script 併用

---

もしよければ次は👇も一緒に整えられるよ：

* Claude Code 用の **`.claude.json` テンプレ**
* Vast/SSH 用の **初期化フルスクリプト**
* 研究用（UniTime）専用の **環境 bootstrap**

どこまで自動化したい？
